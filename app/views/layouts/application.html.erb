<!DOCTYPE html>
<html>
  <head>
    <title>Rides</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>jQuery Mobile: Theme Download</title>
    <link rel="stylesheet" href="http://code.jquery.com/mobile/1.2.0/jquery.mobile-1.2.0.min.css" />
    <script src="http://code.jquery.com/jquery-1.7.1.min.js"></script>
    <script src="http://code.jquery.com/mobile/1.1.1/jquery.mobile-1.1.1.min.js"></script>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div data-role="page" data-theme="b">
      <%= yield %>

      <div data-role="footer" data-theme="a" class="ui-bar">
        <div class="ui-grid-d">
          <div class="ui-block-a"><%= link_to "Home", root_path, "data-icon"=>"home", "data-role"=>"button" %></div>  
          <div class="ui-block-b"><%= link_to "Search", search_rides_path, "data-icon"=>"search", "data-role"=>"button"%></div>
          <% if user_signed_in? %>
            Signed in as <%= current_user.email %>. Not you?
            <div class="ui-block-c"><%= link_to "Sign out", destroy_user_session_path, "data-role"=>"button" %></div>
          <% else %>
            <div class="ui-block-c"><%= link_to "Register", new_user_registration_path, "data-role"=>"button" %></div>
            <div class="ui-block-d"><%= link_to "Sign in", new_user_session_path, "data-role"=>"button" %></div>
          <% end %>
          <div class="ui-block-e"><%= link_to("Connect with Facebook", user_omniauth_authorize_path(:facebook), "data-ajax" => false, "data-role"=>"button") unless session[:fb_access_token] %></div>

        </div>
      </div>
    </div>
  </body>
</html>
